# Database
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# For local development: postgresql://postgres:password@localhost:5432/amuse_bouche
# For Vercel Postgres: Get from Vercel dashboard
DATABASE_URL="postgresql://postgres:password@localhost:5432/amuse_bouche"

# NextAuth v5
# Generate with: npx auth secret
# Or: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here-min-32-chars"

# Application URL (used by NextAuth for callbacks)
# Local: http://localhost:3000
# Production: https://your-domain.vercel.app
AUTH_URL="http://localhost:3000"

# GitHub OAuth Provider
# Create at: https://github.com/settings/developers
# Authorization callback URL: [AUTH_URL]/api/auth/callback/github
AUTH_GITHUB_ID="your-github-oauth-app-client-id"
AUTH_GITHUB_SECRET="your-github-oauth-app-client-secret"

# Google OAuth Provider
# Create at: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs: [AUTH_URL]/api/auth/callback/google
AUTH_GOOGLE_ID="your-google-oauth-client-id"
AUTH_GOOGLE_SECRET="your-google-oauth-client-secret"

# Environment
# Options: development, production, test
NODE_ENV="development"

# Public Environment Variables
# Exposed to the browser - prefix with NEXT_PUBLIC_
# Should match AUTH_URL in most cases
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# GoHighLevel Webhook
# Webhook URL from GoHighLevel for form submissions
GOHIGHLEVEL_WEBHOOK_URL="https://your-webhook-url.gohighlevel.com"
